<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"

    >

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.ridewithme.MapActivity"

        android:layout_above="@+id/searchLocationBtn" />

    <Button
        android:textColor="@color/white"
        android:background="@color/light_blue"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:text="חפש"
        android:id="@+id/searchLocationBtn"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:onClick="onClick"/>

    <EditText
        android:paddingRight="10dp"
        android:alpha="0.7"
        android:background="@color/white"
        android:gravity="center|right"
        android:layout_marginLeft="5dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/searchBoxLocation"
        android:layout_toStartOf="@+id/searchLocationBtn"
        android:layout_alignTop="@+id/searchLocationBtn"
        android:layout_alignParentBottom="true" />



</RelativeLayout>




